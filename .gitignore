# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# Anchored to the repo root so it ignores the legacy top-level kortex/ workspace
# but does NOT touch src/kortex/ (the new Kortex inference frontend orchestrators).
/kortex/

antigravity-workflows/
open-antigravity/
vscodium/
antigravity/
claude-code-fork/

# External reference repositories cloned in for Kortex inference research.
# These ship as separate upstream projects and must NOT be committed here.
ds4/
geometry-of-consolidation/


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
apikeys.txt

# API Keys — NEVER commit
api_keys.json
**/api_keys.json
.kortex/
**/.kortex/

# Rust
src-tauri/target/
src-tauri/zed/
native/
"config/api_keys.json"  
"src-tauri/config/api_keys.json"  
"*.env"  
