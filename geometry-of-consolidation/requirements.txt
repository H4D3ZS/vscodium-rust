# Core runtime deps (pip install -r requirements.txt)
# For a full developer environment, use: pip install -e ".[experiments,modal,vllm,dev]"
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
torch>=2.3
tqdm>=4.66
pyarrow>=15
pandas>=2.2

# Experiments (embeddings, retrieval, plotting)
sentence-transformers>=3.0
datasets>=2.20
hdbscan>=0.8
faiss-cpu>=1.8.0
matplotlib>=3.8
seaborn>=0.13

# Optional: Modal for cloud runs
# modal>=0.64
# boto3>=1.34

# Optional: vLLM for E7 downstream QA (requires CUDA)
# vllm>=0.5.0
