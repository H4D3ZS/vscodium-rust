# Kortex frontend bridge

UI for the kortex Rust workspace (aim-proxy, .aim memory, VFS daemon).
Distinct subsystem — NOT part of the DDD layers (see src/README.md).
Backend counterpart lives in /kortex (separate cargo workspace).
