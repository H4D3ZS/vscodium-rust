# AIRI subsystem

3D VRM avatar + "digital life" companion: consciousness/biology simulation,
autonomous behaviors, voice. Distinct subsystem — NOT part of the DDD layers
(see src/README.md). Heavy deps (three.js, @pixiv/three-vrm) must stay
lazy-loaded so they never land in the entry chunk.
